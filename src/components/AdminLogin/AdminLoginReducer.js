const initialstate = {
    
}

export default function AdminLoginReducer(state = initialstate, action) {
    const { payload, type } = action;

    switch (type){
        default: {
            return state
          }
    }
    
}