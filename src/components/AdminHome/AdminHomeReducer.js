const initialstate = {

}
export default function AdminHomeReducer(state = initialstate, action) {
        const { payload, type } = action;
    
    switch (type){
        default: {
            return state
          }
    }

}
