const initialstate = {

}
export default function OrganizersReducer(state = initialstate, action) {
    const { payload, type } = action;
    
    switch (type){
        default: {
            return state
          }
    }

}
